import {juggler} from '@loopback/repository';
const dsConfig = require('./<%= jsonFileName %>');

export class <%= name %>DataSource extends juggler.DataSource {
  constructor() {
    super(dsConfig);
  }
}
